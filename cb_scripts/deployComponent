# {
#   "name": "deployComponent",
#   "description": "Deploy practifi component json to the default org",
#   "usage": "dev deployComponent <componentPath> [alias]"
# }
if [ -z $1 ]; then
    echo 'Component path is required'
    exit 0
fi

alias=${2:-$DefaultAlias}

sf mypim run script -o $alias -f ~/DevOps/cb_js/deployComponent.js $1
