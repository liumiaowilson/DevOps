# {
#   "name": "exportOrgHub",
#   "description": "Export the connection in MyPIM OrgHub to local alias",
#   "usage": "dev exportOrgHub <name>"
# }
name=$1

sf mypim export orghub -n $name -a $name
