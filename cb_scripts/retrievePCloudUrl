# {
#   "name": "retrievePCloudUrl",
#   "description": "Retrieve the pCloud public URL for an Item__c.File_1__c value",
#   "usage": "dev retrievePCloudUrl <recordId>"
# }
recordId=$1

if [ -z $recordId ]; then
    echo 'Record Id is required'
    exit 0
fi

sf mypim run script -o $DefaultAlias -f ~/DevOps/cb_js/retrievePCloudUrl.js $recordId
