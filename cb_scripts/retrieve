# {
#   "name": "retrieve",
#   "description": "Retrieve all metadata to the current directory",
#   "usage": "dev retrieve ."
# }
dir=$1

sf project retrieve start -o $DefaultAlias -c -x "$1"/package.xml
